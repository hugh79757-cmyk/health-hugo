# .continue-here.md — Compass 재생성 완료 상태

생성일: 2026-09-23

---

## Current Position

Compass 프로젝트 — 39편 재생성 완료, swap 검증 대기 중.

현재 위치: `health-hugo/compass/` 디렉토리. 기존 파이프라인 수정 없이 독립 동작.

---

## Completed This Session

### 1. T5.2 모드별 H2 pool 분기 (이전 세션, now reverted)
- `health_post_processor.py` — T5.2 pool split 구현했다가 사용자 요청으로 원상복구
- 기존 파이프라인 수정 금지 확인 — compass/ 독립 경로로 전환

### 2. compass/ 독립 구현 (7파일)
- `compass_runner.py` — CLI 엔트리 (--keyword, --mode, --real-llm, --evaluate)
- `compass_prompts.py` — 4패턴 A/B/C/D 프롬프트 (311 lines)
- `compass_generator.py` — shared.ai_writer 직접 호출 (273 lines)
- `compass_post_processor.py` — 독립 후처리 (117 lines)
- `compass_evaluator.py` — 5항목 자동평가 (151 lines)
- `compass_claim_scanner.py` — 6개 금지패턴 (51 lines)
- `config/modes.yaml` — 모드 정의 + 가중치

### 3. health_conditions.yaml 데이터 수정
- 18개 required_form: rTG → 올바른 형태로 수정
- 프로폴리스 key_nutrients: "클로메틴" → "비타민P"
- `required_form` 참조 코드 1건 (health_enricher.py:159) — decision A (원본 수정) 적용

### 4. 4패턴 프롬프트 재설계
- 패턴 A: 성분 중심 진입
- 패턴 B: 증상 중심 진입
- 패턴 C: 가격 중심 진입
- 패턴 D: 상황 중심 진입
- `hashlib.md5(keyword) % 4` 으로 키워드별 패턴 결정
- 타겟: 10,000-14,000자

### 5. 39편 재생성 — 전편 PASS
- 13개 키워드 × 3모드 = 39편
- compliance: 39/39 PASS (100%)
- entropy: 최소 2.585 (평균 ~3.3)
- 평균 글자수: 10,372자
- 글자수 분포: <7K=2, 7-10K=25, 10-15K=5, 15K+=7

---

## Remaining Work

### swap 검증 기준 (사용자가 정의)
1. 30편 이상 생성 + 평가 → ✅ 39편
2. Compliance 100% PASS → ✅
3. Entropy ≥ 2.5 → ✅
4. 10편 이상 수동 리뷰 → ❌ 미수행
5. CTR/RPM 비교 → ❌ 미수행 (프로덕션 투입 후)

### 수동 리뷰 필요
- 10편 선정 (대표 키워드 × 3모드)
- 성분 정확성, affiliate 자연스러움, 읽기 편의성 확인
- 기존 production 글과 섹션 대비 확인

### 추가 개선 가능
- 7K 미만 2편 재시도 (홍삼_versus, 콜라겐_ingredient_dive)
- LLM rotation 문제 해결 (or-nexmini hanging)

---

## Decisions

1. **기존 파이프라인 수정 금지** — 사용자 강력 지시. compass/는 독립 디렉토리
2. **shared.ai_writer만 허용** — compass_generator.py에서 직접 호출. writer.py 우회
3. **4패턴 rotation** — hashlib.md5 기반 결정적 패턴 선택. H2 구조 다양성 확보
4. **required_form 원본 수정** — health_enricher.py에서만 1회 참조, 영향 없음
5. **comparator 모드 vs augmented 모드** — 비교 추천 형식 유지 (ingredient_dive/symptom_care/versus)

---

## Bugs & Issues Discovered

### 1. LLM rotation hanging
- **증상**: ai_writer.generate()가无限 hanging
- **원인**: tier rotation state가 `or-nexmini`(openrouter)로 이동 → openrouter API timeout
- **해결책**: `data/llm_rotation_state.json`을 `{"front": "groq-qwen"}로 리셋
- **미해결**: openrouter free 모드 불안정. next session에서 자동 재발 가능

### 2. compass_generator sys.path
- **증상**: import 실패
- **원인**: 상대 경로 `../5000`이 실패
- **해결책**: 하드코딩 `/Users/twinssn/Projects/5000` 사용

### 3. 빈 H2 섹션 (구버전 프롬프트)
- **증상**: 15K+ 글자수인데 빈 H2 있음
- **원인**: awk 스크립트가 H3를 "빈 content"로 오인
- **해결책**: 실제로는 H3 하위에 풍부한 내용 존재. 4패턴 프롬프트에서 해결

---

## Environment Notes

- **LLM tier order**: groq-qwen → groq-gpt120b → ... → or-nexmini (openrouter, 불안정)
- **rotation state 파일**: `/Users/twinssn/Projects/5000/data/llm_rotation_state.json`
- ** compass 출력**: `compass/output/drafts/` (39 .md) + `compass/output/evaluated/` (39 .json)
- **기존 drafts backup**: `compass/output/drafts/backup/`
- **health_conditions.yaml 수정**: 원본 수정됨 (18개 required_form)
- **API keys**: 환경변수에서 로드 (GROQ_API_KEY_*, OPENROUTER_API_KEY 등)

---

## Resume Instructions

```bash
cd /Users/twinssn/Projects/CUAP/health-hugo

# compass 동작 확인
python3 compass/compass_runner.py --keyword 루테인 --mode versus --real-llm

# 7K 미만 2편 재시도
echo '{"front": "groq-qwen"}' > /Users/twinssn/Projects/5000/data/llm_rotation_state.json
python3 compass/compass_runner.py --keyword 홍삼 --mode versus --real-llm
echo '{"front": "groq-qwen"}' > /Users/twinssn/Projects/5000/data/llm_rotation_state.json
python3 compass/compass_runner.py --keyword 콜라겐 --mode ingredient_dive --real-llm

# 전체 재평가
python3 compass/compass_runner.py --evaluate compass/output/drafts/

# 수동 리뷰 시작
# 10편 선정 후 .planning/writing-modes/MANUAL-REVIEW.md 작성
```

---

## Uncommitted Changes

```
 M .DS_Store
 M .planning/STATE.md
 M .wrangler/cache/pages.json
 M .wrangler/cache/wrangler-account.json
 M config/_default/hugo.toml
 M config/_default/params.toml
 M layouts/partials/adsense/in-article.html
 M layouts/partials/extend-head.html
 D layouts/partials/extend_head.html
A  pipelines/curation/health_post_processor.py
A  scripts/expression_tracker.py
 M wrangler.toml
```

compass/ 디렉토리는 untracked (git에 추가 안 됨).
